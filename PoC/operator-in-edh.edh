{

  // Some fancy chars to be used as operators
  //
  // ++ "⁉️❇️‼️♾️☢️☣️™️©️®️⚡▢◆◇◈◉◎☆★☼♻⎔⌬⏣♠️♥️♦️♣️▲▶▼◀"
  // ++ "🔀🔁🔂⏩⏪🔼⏫🔽⏬⏹️"
  // ++ "🌀📢📣🔔🔕🎵⭕"
  // ++ "🆔🆘🆚"

  operator != (lhv, rhv) not (lhv == rhv)

  // TODO consider not including (!~=) in default batteries,
  // to discourage deep-value based equality test over all
  operator !~= 4 (lhv, rhv) not (lhv ~= rhv)

  operator &= (lhv, rhv) {
    if lhv != false && lhv != nil && lhv != ''
      then rhv
      else nil
  }

  operator |= (lhv, rhv) {
    if lhv == false || lhv == nil || lhv == ''
      then rhv
      else nil
  }

}
